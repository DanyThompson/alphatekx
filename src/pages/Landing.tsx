import { useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import { ArrowRight, Blocks, Eye, Sparkles, WandSparkles } from 'lucide-react'

export default function Landing(){const [prompt,setPrompt]=useState('');const nav=useNavigate();const go=()=>{if(prompt)try{window.localStorage.setItem('alphatekx-draft',prompt)}catch{}nav('/builder')};return <div className="min-h-screen px-5 py-4"><nav className="glass mx-auto flex max-w-6xl items-center justify-between rounded-full px-5 py-3"><Link to="/" className="flex items-center gap-2 text-lg font-bold"><Sparkles className="text-blue-600"/>Alphatekx</Link><div className="flex items-center gap-2"><Link to="/home" className="hidden rounded-full px-4 py-2 text-sm sm:block">Workspace</Link><Link to="/builder" className="rounded-full bg-slate-900 px-5 py-2.5 text-sm font-semibold text-white">Start building</Link></div></nav><main className="mx-auto max-w-6xl pb-16 pt-24 text-center"><div className="mx-auto mb-6 flex w-fit items-center gap-2 rounded-full border border-blue-200 bg-blue-50/80 px-4 py-2 text-sm text-blue-700"><WandSparkles size={15}/> No code. Just imagination.</div><h1 className="mx-auto max-w-4xl text-5xl font-bold leading-[1.08] tracking-tight md:text-7xl">Turn your ideas<br/><span className="bg-gradient-to-r from-blue-600 via-violet-600 to-cyan-500 bg-clip-text text-transparent">into reality</span></h1><p className="mx-auto mt-6 max-w-2xl text-lg leading-8 text-slate-600">Describe the product you want. Alphatekx plans, codes, and previews a working application in moments.</p><div className="glass mx-auto mt-10 flex max-w-[600px] items-center rounded-full p-2"><input value={prompt} onChange={e=>setPrompt(e.target.value)} onKeyDown={e=>e.key==='Enter'&&go()} className="min-w-0 flex-1 bg-transparent px-5 py-3 outline-none" placeholder="Describe your app idea..."/><button onClick={go} className="flex items-center gap-2 rounded-full bg-blue-600 px-6 py-3 font-semibold text-white shadow-lg shadow-blue-200">Build <ArrowRight size={17}/></button></div><div className="mt-20 grid gap-5 text-left md:grid-cols-3">{[[Blocks,'Real applications','Generate functional React apps, not static mockups.'],[Eye,'Live preview','See and use your product immediately as it is created.'],[Sparkles,'Made for everyone','Go from plain language to polished software without setup.']].map(([I,t,d])=><article className="glass p-7" key={t as string}><I className="text-blue-600"/><h2 className="mt-5 text-lg font-bold">{t as string}</h2><p className="mt-2 text-sm leading-6 text-slate-600">{d as string}</p></article>)}</div></main></div>}
